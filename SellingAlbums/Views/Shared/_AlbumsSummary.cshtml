@model Album

<div class="col-sm-4 col-md-4 col-lg-4">
    <p><img src="@Model.CoverUrl" class="w-75 img-fluid" /></p>
    <h3>@Model.Price.ToString("c")</h3>
    <h4>@Model.Title</h4>
    <p>@Model.Artist.Name</p>
</div>